{
    "author": "Dennis Kempin <dennis.kempin@gmail.com>",
    "name": "sres",
    "version": "0.1.0",
    "scripts": {
        "build": "rimraf dist && webpack --mode production --no-stats",
        "build-dev": "rimraf dist && webpack --mode development",
        "serve": "rimraf dist && webpack-dev-server --mode development --no-stats",
        "serve-release": "rimraf dist && webpack-dev-server --mode production"
    },
    "devDependencies": {
        "@wasm-tool/wasm-pack-plugin": "^1.6.0",
        "@webpack-cli/serve": "^3.0.1",
        "copy-webpack-plugin": "^12.0.2",
        "rimraf": "^6.0.1",
        "wasm-opt": "^1.3.0",
        "webpack": "^5.74.0",
        "webpack-cli": "^6.0.1",
        "webpack-dev-server": "^5.2.0"
    }
}
